{
  "name": "stack-overflow-survey",
  "version": "1.0.0",
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:api": "cross-env PYTHONPATH=backend PYTHONUTF8=1 fastapi dev backend/app/main.py --port 8000",
    "dev": "npm-run-all --parallel dev:api dev:frontend"
  },
  "devDependencies": {
    "cross-env": "^10.0.0",
    "npm-run-all": "^4.1.5"
  }
}
